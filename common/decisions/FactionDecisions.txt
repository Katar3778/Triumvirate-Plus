
ITA_triumvirateplus_category_faction = {
	
	ITA_triumvirateplus_invite_spain = {
	
		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
			}
			NOT = {
				is_in_faction_with = GER
			}
			
			is_ally_with = TUR
			is_faction_leader = yes
		}
		cost = 0 
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
			SPA = { country_event = trfaction.1 }
		}
		timeout_effect = {
			SPA = { country_event = trfaction.1 }
		}
	}
}