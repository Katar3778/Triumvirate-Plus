
ITA_triumvirateplus_category_europe = {

    ITA_triumvirateplus_independentstateofserbia = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
			}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				107 = { is_controlled_by_ROOT_or_subject = yes }
				108 = { is_controlled_by_ROOT_or_subject = yes }	
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = ISE autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 107
						state = 108
				    }
			    }

			    ISE = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}

			    }
		    }
		}
	}
    ITA_triumvirateplus_governorateofmontenegro = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				105 = { is_controlled_by_ROOT_or_subject = yes }	
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
			}
		}
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IMT autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 105
				    }
			    }

			    IMT = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_hellenicstate = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
			}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				185 = { is_controlled_by_ROOT_or_subject = yes }
				186 = { is_controlled_by_ROOT_or_subject = yes }
				47 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IGR autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 185
						state = 186
						state = 47
				    }
			    }

			    IGR = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}

			    }
		    }
		}
	}

    ITA_triumvirateplus_kingdomofalbania = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
			}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				934 = { is_controlled_by_ROOT_or_subject = yes }
				805 = { is_controlled_by_ROOT_or_subject = yes }
				44 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IAL autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 934
						state = 805
						state = 44
				    }
			    }

			    IAL = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}

			    }
		    }
		}
	}

    ITA_triumvirateplus_independentstateofcroatia = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				109 = { is_controlled_by_ROOT_or_subject = yes }	
				104 = { is_controlled_by_ROOT_or_subject = yes }
				804 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = ICR autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 109
						state = 104
						state = 804
				    }
			    }

			    ICR = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}
}

ITA_triumvirateplus_category_africa = {

    ITA_triumvirateplus_kingdom_of_egypt = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				452 = { is_controlled_by_ROOT_or_subject = yes }
				447 = { is_controlled_by_ROOT_or_subject = yes }
				907 = { is_controlled_by_ROOT_or_subject = yes }
				446 = { is_controlled_by_ROOT_or_subject = yes }
				453 = { is_controlled_by_ROOT_or_subject = yes }
				457 = { is_controlled_by_ROOT_or_subject = yes }
				552 = { is_controlled_by_ROOT_or_subject = yes }
				456 = { is_controlled_by_ROOT_or_subject = yes }
				767 = { is_controlled_by_ROOT_or_subject = yes }
				551 = { is_controlled_by_ROOT_or_subject = yes }
				883 = { is_controlled_by_ROOT_or_subject = yes }
				887 = { is_controlled_by_ROOT_or_subject = yes }
				886 = { is_controlled_by_ROOT_or_subject = yes }
				884 = { is_controlled_by_ROOT_or_subject = yes }
				885 = { is_controlled_by_ROOT_or_subject = yes }
				549 = { is_controlled_by_ROOT_or_subject = yes }	
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IEG autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 452
						state = 447
						state = 446
						state = 453
						state = 457
						state = 552
						state = 456
						state = 767
						state = 551
						state = 883
						state = 887
						state = 886
						state = 884
						state = 885
						state = 549
						state = 907
				    }
			    }

			    IEG = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}

			    }
		    }
		}
	}

    ITA_triumvirateplus_kingdomoftunisia = {

		
	icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				665 = { is_controlled_by_ROOT_or_subject = yes }	
				458 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = ITU autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 665
						state = 458
				    }
			    }

			    ITU = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_italianalgeria = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				459 = { is_controlled_by_ROOT_or_subject = yes }	
				460 = { is_controlled_by_ROOT_or_subject = yes }
				513 = { is_controlled_by_ROOT_or_subject = yes }
				514 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = ILG autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 459
						state = 460
						state = 513
						state = 514
				    }
			    }

			    ILG = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_italianlibya = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				448 = { is_controlled_by_ROOT_or_subject = yes }	
				449 = { is_controlled_by_ROOT_or_subject = yes }
				450 = { is_controlled_by_ROOT_or_subject = yes }
				451 = { is_controlled_by_ROOT_or_subject = yes }
				661 = { is_controlled_by_ROOT_or_subject = yes }
				662 = { is_controlled_by_ROOT_or_subject = yes }
				663 = { is_controlled_by_ROOT_or_subject = yes }
				273 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 300
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = ILB autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 448
						state = 449
						state = 450
						state = 451
						state = 661
						state = 662
						state = 663
						state = 273
				    }
			    }

			    ILB = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_italiancentralafrica = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				775 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = ICH autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 775
				    }
			    }

			    ICH = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_italianniger = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				515 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = INR autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 515
				    }
			    }

			    INR = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}
}
ITA_triumvirateplus_category_asia = {

    ITA_triumvirateplus_governorateofthegulf = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				658 = { is_controlled_by_ROOT_or_subject = yes }	
				765 = { is_controlled_by_ROOT_or_subject = yes }

			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = GUL autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 658
						state = 765
				    }
			    }

			    GUL = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_kingdomofsaudiarabia = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				854 = { is_controlled_by_ROOT_or_subject = yes }	
				855 = { is_controlled_by_ROOT_or_subject = yes }
				679 = { is_controlled_by_ROOT_or_subject = yes }
				855 = { is_controlled_by_ROOT_or_subject = yes }
				857 = { is_controlled_by_ROOT_or_subject = yes }
				856 = { is_controlled_by_ROOT_or_subject = yes }
				858 = { is_controlled_by_ROOT_or_subject = yes }
				859 = { is_controlled_by_ROOT_or_subject = yes }
				678 = { is_controlled_by_ROOT_or_subject = yes }
				292 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = ISA autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 854
						state = 855
						state = 679
						state = 855
						state = 857
						state = 856
						state = 858
						state = 859
						state = 678
						state = 292
				    }
			    }

			    ISA = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_kingdomofyemen = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				293 = { is_controlled_by_ROOT_or_subject = yes }	
				659 = { is_controlled_by_ROOT_or_subject = yes }

			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IYE autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 293
						state = 659
				    }
			    }

			    IYE = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_kingdomofoman = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = {
			    tooltip = trsplus.tooltip.1
				294 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IOM autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 294
				    }
			    }

			    IOM = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_governorateofpalestine = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = {
			    tooltip = trsplus.tooltip.1
				454 = { is_controlled_by_ROOT_or_subject = yes }
			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IPA autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 454
				    }
			    }

			    IPA = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}

    ITA_triumvirateplus_emirateoftransjordan = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}

			custom_trigger_tooltip = { 
			    tooltip = trsplus.tooltip.1
				455 = { is_controlled_by_ROOT_or_subject = yes }	

			}

		}

		cost = 0
		ai_will_do = {
			factor = 10
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
				}
		}
		
		fire_only_once = yes

		complete_effect = {
		    if = {
			add_political_power = 25
				add_stability = 0.01

				set_autonomy = { target = IJO autonomy_state = autonomy_reichskommissariat}
			}

			every_state = {
			    limit = {
				    OR = {
						state = 455
				    }
			    }

			    IJO = {

				    transfer_state = PREV
					
					hidden_effect = {
				        add_state_core = PREV
					}
			    }
		    }
		}
	}
}

ITA_triumvirateplus_category_uniting_pac = {

    ITA_triumvirateplus_unite_governorateofthelevant = {

		icon = ger_reichskommissariats

		available = {
			OR = {
				has_government = fascism
				has_government = neutrality
			}
			country_exists = IPA
			country_exists = IJO
		
            custom_trigger_tooltip = { 
			    tooltip = trsunited.unitetooltip.1
				IPA = { has_country_flag = IPA_territory_decision }
				IJO = { has_country_flag = IJO_territory_decision }
			}			
		}

		cost = 0
		
		ai_will_do = {
			factor = 0
		}

		visible = {
			OR = {
				has_government = fascism
				has_government = neutrality
			}
			country_exists = IPA
			country_exists = IJO
		}
		
		fire_only_once = yes

		complete_effect = {
			country_event = trsunited.1
		}
		timeout_effect = {
			country_event = trsunited.1
		}
	}
}
